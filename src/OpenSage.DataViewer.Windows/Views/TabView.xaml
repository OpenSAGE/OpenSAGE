<UserControl x:Class="OpenSage.DataViewer.Views.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0"
                 BorderThickness="0"
                 Style="{StaticResource VirtualisedMetroListBox}"
                 ItemsSource="{Binding Files}"
                 DisplayMemberPath="FilePath"
                 SelectedItem="{Binding SelectedFile}" />

        <GridSplitter Grid.Column="1" Width="5" ResizeBehavior="PreviousAndNext" />

        <Border Grid.Column="2" Padding="20 20 5 20" BorderBrush="LightGray" BorderThickness="1">
            <ContentControl cal:View.Model="{Binding SelectedFileContent}" />
        </Border>
    </Grid>
</UserControl>
